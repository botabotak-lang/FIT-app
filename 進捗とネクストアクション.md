# 船舶修理作業報告アプリ 進捗とネクストアクション

**最終更新**: 2026/04/16  
**用途**: 修理アプリの状態とネクストを記録。勤怠MVPは一段落（別リポジトリ）。戻ってきたときに忘れないように。

**Supabase 案件テーブル**: 本番 DB で `supabase/ship_cases.sql` を一度実行（`customers_employees` と同じプロジェクト）。

**FIT 向け納品の注意（両アプリ）**: [`FIT_納品メモ_両アプリ.md`](../../../docs/30_Operations/FIT_納品メモ_両アプリ.md) を参照。

---

## 📦 製品マスタ（FIT・マスタ整備の定義）

**状態（2026/04）**: **シート整備・運用上のマスタは完了**済み。以下の列定義は、アプリの `Material` / `productMaster` と突き合わせる際の**参照用**として残す。

| 列 | 内容 |
|:---|:---|
| **品名** | 商品名称 |
| **型式（規格）** | 型番・規格 |
| **仕入先** | 発注・仕入先 |
| **仕入値** | 仕入単価または原価 |
| **売値** | 販売単価 |
| **備考** | 補足メモ |

※アプリ現状の `Material` / `productMaster` は数量・送料などもあるため、インポート時はマッピング方針を決める。

---

## 📍 現在の進捗サマリー

### ✅ 完了済み（MVP）

| 項目 | 状態 | 備考 |
|:---|:---|:---|
| **Phase 1.5.1** | ✅ 完了 | ステップ式UI、基本情報の重複入力排除 |
| **単価入力バグ修正** | ✅ 完了 | 仕入単価・売値単価の自由入力・全選択対応 |
| **MVP（見積書・請求書）** | ✅ 完了 | Step 5追加、PDF/Excel出力、製品マスタ10品目 |
| **製品マスタ（FITシート／列定義）** | ✅ 完了 | 事務・現場向けシート整備済み。アプリは `lib/productMaster.ts` 等で運用。インポート時のマッピングは別タスクで十分 |

### 実装済み機能一覧

- **5ステップのウィザード**
  1. 基本情報（顧客・船名・科目・型名・完成日）
  2. 作業者選択
  3. 作業報告書（時間×単価の自動計算：平日7000円、休日8400円、移動0.8倍）
  4. 材料持出表（製品マスタから単価自動入力、履歴学習、粗利計算）
  5. **見積書・請求書プレビュー** → PDF出力（印刷）/ Excel出力

- **製品マスタ（仮）**: 10品目（船舶用VHF、GPSアンテナ、レーダー、航海灯、バッテリー等）
- **会社情報**: `lib/types.ts` の `COMPANY_INFO` に仮データ（FITヒアリング後に差し替え）

### デプロイ

- **Vercel** にデプロイ済み（GitHub連携）
- リポジトリ: `https://github.com/botabotak-lang/FIT-app.git`

---

## 🎯 ネクストアクション（修理アプリに戻ったら）

**2026/3/21 方針の全体像**: 親フォルダ `../../docs/00_Meta/FIT_開発記録_20260321.md` を参照（時間ブロック自由追加・船ベース・製品マスタ列など）。

### 優先度：高（直近の改修）

| No | タスク | 詳細 |
|:---|:---|:---|
| 0 | **作業時間のブロック化** | ✅ 2026/3/21 実装済。`TimeBlock[]`・localStorage旧データは `normalizeShipCase` で移行 |
| 1 | **Supabase導入** | データ永続化、複数端末共有 |
| 2 | **DB保存の本番化** | localStorage 案件をDBへ（一覧はホームで実装済み） |
| 3 | **会社情報の差し替え** | FITヒアリングで取得した正式情報を `lib/types.ts` に反映 |

### ✅ 2026/04/06 反映（社員要望・軽改修バッチ）

| 項目 | 内容 |
|:---|:---|
| **① 1枚目→スプレッドシート編集** | Phase1として **Excel（.xlsx）で1枚目相当を出力**（`WorkReportStep`）。**印刷**は補助ボタンで継続。 |
| **③ 作業内容の改行** | 入力は `textarea` で改行保持。**印刷HTML**は `white-space: pre-wrap` で改行表示。 |
| **④ 作業内容列の幅** | 印刷用テーブルで時間・作業者・場所を圧縮し **作業内容列を広げる**（`table-layout: fixed`）。 |
| **材料追加ボタン位置** | Step4 `MaterialsStep`：**「+ 材料追加」を材料一覧の最下部**へ。 |
| **一覧の日付** | ホーム案件行の日付表示を **`updatedAt` → `basicInfo.receptionDate`**（未入力は「—」）。 |

### ⏸ クローズ／保留

| 論点 | 状態 |
|:---|:---|
| **② 重複案件** | **運用（操作ミス）と判断**・仕様変更は見送り。 |
| **⑤ PC/スマホ同期** | **Supabase / DB本番化**で対応（優先度：高の別タスク）。 |
| **修理報告「2枚目」** | **FIT から依頼・様式が出るまで対象外**（営業タスク表の行も参照）。 |

### 優先度：中（Phase 2.5〜3）

| No | タスク | 詳細 |
|:---|:---|:---|
| 5 | **スプシ連携** | 製品マスタをGoogleスプレッドシートで管理→DB同期 |
| 6 | **顧客マスタ化** | 顧客情報をDB管理 |
| 7 | **Excel/CSVエクスポート強化** | 経理向けの微調整用 |
| 8 | **月次集計レポート** | 売上・工賃・粗利の可視化 |

### 営業・確認タスク（開発外）

| 期限 | 内容 |
|:---|:---|
| **—** | **修理報告「2枚目」**: 2026/04 時点で **FIT から様式・開発依頼はなし**。依頼が出るまで**追わない**（スコープ外）。出た時点で見積・優先度を決める。 |

### 細かい修正タスク（まとめて実施）

| No | 対象ファイル | 内容 |
|:---|:---|:---|
| 1 | `app/page.tsx` | ✅ 2026/04/06 案件一覧の日付表示を `basicInfo.receptionDate`（未入力は「—」）に変更 |

---

### 2026年○月○日 面談で決定した追加要件

| No | 要件 | 詳細 |
|:---|:---|:---|
| 1 | **作業報告書の打ち出し** | 現状ないので実装してほしい |
| 2 | **作業報告内容の記入欄** | 追加してほしい |
| 3 | **作業報告データの一覧表示** | 船名をベースに一覧で見れるように |
| 4 | **2枚目の修理報告書の出力** | 面談メモ上は必要だが、**FIT から依頼・様式が出るまで開発ネクストに含めない** |
| 5 | **入力項目の予測変換** | 一度入力したら登録され、次回から予測変換のように使えるように |

### FITヒアリングで確認したいこと（未実施分）

- 会社情報（正式な住所・電話・振込先）
- 見積書・請求書の締め日・発行ルール
- 製品マスタ：シート整備は済み。**価格改定の運用**（頻度・誰がアプリ／シートを更新するか）が必要になれば確認
- 現場での使用感フィードバック

---

## 📋 ビジネス前提（忘れないように）

- **月額5万円**（HP運用費＋業務改善コンサル）でFITに提供中（2026年○月 了承済み）
- **含まれる範囲**: 月1回の打ち合わせ、軽微な改修、バグ修正
- **勤怠システム**: 別契約（一括 or 月額追加）。5万円には含めない
- **スプシ連携**: 将来的に可能。Phase 2.5で検討

---

## 📂 重要ファイル

| ファイル | 用途 |
|:---|:---|
| `app/page.tsx` | メイン画面、5ステップの制御 |
| `components/steps/` | 各ステップのコンポーネント |
| `lib/types.ts` | 型定義、定数、会社情報 |
| `lib/productMaster.ts` | 製品マスタ（仮10品目） |
| `PHASE2_TASKS.md` | Phase 2以降のタスク詳細 |

---

## 🔗 関連プロジェクト

- **勤怠システム**: 別開発。同じFIT向け・同じチャットで進める想定
- **FIT_PROJECT_MAIN.md**: FIT事業全体の管理シート

---

## 次フェーズ実装メモ（2026/04/15 合意プラン）

**前提**: 以下のコード変更は Cursor の **Agent モード**で行う（Plan モードでは `.md` 以外を編集できないため）。

### 1. Supabase（SQL Editor で1回実行）

`customers` / `employees` テーブル＋RLS＋初期行。`products` と同じプロジェクトで実行。

```sql
create table if not exists customers (
  id uuid default gen_random_uuid() primary key,
  name text not null unique,
  address text not null default '',
  phone text not null default '',
  notes text not null default '',
  sort_order integer not null default 0,
  is_active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
alter table customers enable row level security;
drop policy if exists "allow_all_anon_customers" on customers;
create policy "allow_all_anon_customers" on customers for all to anon using (true) with check (true);

create table if not exists employees (
  id uuid default gen_random_uuid() primary key,
  name text not null unique,
  sort_order integer not null default 0,
  notes text not null default '',
  is_active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
alter table employees enable row level security;
drop policy if exists "allow_all_anon_employees" on employees;
create policy "allow_all_anon_employees" on employees for all to anon using (true) with check (true);

insert into customers (name, sort_order) values
  ('東海汽船', 0), ('清水港運', 1), ('焼津漁協', 2), ('鈴与海運', 3), ('その他', 4)
on conflict (name) do nothing;
insert into employees (name, sort_order) values
  ('大竹', 0), ('豊島', 1), ('鈴木', 2), ('内田', 3), ('新人', 4)
on conflict (name) do nothing;
```

### 2. アプリ実装タスク（チェックリスト）

| # | 内容 |
|:---|:---|
| 1 | 新規 `lib/customerMaster.ts` / `lib/employeeMaster.ts`（`productMaster.ts` と同型の CRUD） |
| 2 | `lib/types.ts` の `Worker` を **`string` 型**に変更。`WORKERS` 定数は削除（マスタから取得） |
| 3 | `app/customers/page.tsx` / `app/employees/page.tsx` ＋フォームダイアログ（製品マスタ踏襲） |
| 4 | `app/page.tsx` に「顧客マスタ」「社員マスタ」導線 |
| 5 | `BasicInfoStep.tsx`：`getActiveCustomers()` で Select。マスタにない顧客名は「その他」相当で手入力 |
| 6 | `WorkerSelectionStep.tsx` / `WorkReportStep.tsx`：`getActiveEmployees()` で一覧。既存案件の `worker` がマスタ外でも表示できるよう **SelectItem を補完** |
| 7 | `lib/workReportLayout.ts` を新設し、**印刷HTMLとExcelの行組み立てを共通化**（`WorkReportStep` から呼ぶ） |
| 8 | レガシー `WorkReportForm.tsx`：`Worker` ユニオン依存を `Record<string, TimeSlot[]>` 等に置換（コンパイル通し） |

### 3. プラン詳細（Cursor 内）

`/Users/kubotakeisuke/.cursor/plans/顧客・社員マスタ_6bdfe77f.plan.md` を参照。
