# 船舶修理報告アプリ（ship-repair-app）納品チェックリスト

[FIT_納品メモ_両アプリ.md](../../../docs/30_Operations/FIT_納品メモ_両アプリ.md) と併用する。本番 URL はデプロイ後に記入する。

**ビルド確認（開発）**: 2026-04-17 時点でリポジトリ内で `npm run lint` / `npm run build` が成功することを確認済み。

---

## 1. Supabase（顧客・社員・製品マスタと同一プロジェクト）

- [ ] SQL Editor で [supabase/ship_cases.sql](../supabase/ship_cases.sql) を **1回** 実行した
- [ ] 既に `customers` / `employees` / `products` 用 SQL を同一プロジェクトで実行済みである

---

## 2. Vercel

- [ ] **Environment Variables** に以下を設定した  
  - `NEXT_PUBLIC_SUPABASE_URL`  
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`（**anon**。`service_role` はブラウザに入れない）
- [ ] 環境変数変更後に **Redeploy** した
- [ ] 本番 URL: `________________________`

---

## 3. 本番スモークテスト（2端末またはシークレット＋通常）

- [ ] トップで案件一覧が表示される（エラー文言が出ない）
- [ ] 新規案件を作成し、**保存**が成功する
- [ ] 別ブラウザまたは別端末で同じ案件が表示される（Supabase 共有の確認）
- [ ] ホームに **「ブラウザ内の旧データ」** バナーが出る場合、手順どおり **クラウドに取り込む** を試し、二重表示が解消する

---

## 4. FIT 向けセキュリティ説明（合意用）

以下を口頭またはメールで共有し、必要なら FIT 側の確認欄に記入する。

- [ ] **説明済み**: 案件テーブルは現状 **anon キー＋ RLS 全許可** のため、**本番 URL と anon キーが第三者に漏れると案件データの読み書きが可能**になる。URL は社内限定で共有し、キーはクライアントに埋め込まれる前提で管理する。
- [ ] より厳密な認証が必要になった場合は、別途 Supabase Auth やサーバー経由への移行を検討する旨を伝えた

**FIT 確認（任意）**: 氏名・日付 `________ / ________`

---

## 5. 会社情報・スコープ（合意用）

- [ ] **説明済み**: 見積・請求に載る会社情報（`lib/types.ts` の `COMPANY_INFO`）は、住所・電話・振込先など **仮／プレースホルダ** が含まれる可能性がある。FIT ヒアリングで正式データが揃い次第、開発側で差し替える。
- [ ] **説明済み**: 修理報告「2枚目」は FIT から様式・開発依頼が出るまで **スコープ外**（合意どおり）である。
- [ ] **説明済み**: 面談で挙がった「入力の予測変換」は、材料の商品名など **ブラウザ履歴・マスタ選択** で一部対応している。全項目を横断した予測変換を追加するかは、利用後の要望で優先度を決める。

**FIT 確認（任意）**: 氏名・日付 `________ / ________`

---

## 6. 納品完了の目安

上記 1〜3 がすべてチェックできれば、[FIT_納品メモ_両アプリ.md](../../../docs/30_Operations/FIT_納品メモ_両アプリ.md) の「修理：本番 URL で案件の一覧・編集・保存ができ、複数端末で共有できる」に相当する。
